<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="https://cdn.bootcss.com/bootstrap/3.3.6/css/bootstrap.min.css">
    <script src="https://cdn.bootcss.com/vue/2.4.4/vue.min.js" charset="utf-8"></script>
    <script src="https://cdn.bootcss.com/axios/0.16.2/axios.min.js" charset="utf-8"></script>
  </head>
  <body>
    <div id="app">
      <div class="container">
        <div class="col-md-8 col-md-offset-2">
          <h1>新建文章</h1>
          <div class="form-group">
            <input type="text" class="form-control" v-model="title" placeholder="请输入文章标题">
          </div>
          <div class="form-group">
            <textarea class="form-control" rows="8" cols="80" v-model="content" placeholder="请输入文章内容"></textarea>
          </div>
          <div class="form-group">
            <button class="btn btn-default" name="button" v-on:click="submit">提交</button>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
<!-- 将文章内容提交到服务器 -->
<script>
var vm = new Vue({
  el: "#app",
  data:{
    title: '',
    content: ''
  },
  methods:{
    submit(){
      axios.post('/api/posts/create', {
        title: vm.title,
        content: vm.content
      })
      .then(function(response){
        alert(JSON.stringify(response.data));
      })
      .catch(function(err){
        alert(err);
      })
    }
  }
}
);
</script>
